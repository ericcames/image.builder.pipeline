---
collections:
  - name: amazon.aws
    version: "11.2.0"

  # OpenShift / KubeVirt — the Windows containerDisk build (#24) drives plain
  # VirtualMachine objects through the API, the same way this repo drives EC2
  # with amazon.aws. Pinned to match sales.demos so the producer and consumer
  # cannot disagree about the client library.
  - name: kubernetes.core
    version: "6.4.0"

  # CIS L1 hardening (#24 PR 2) — the ansible-lockdown/Windows-2022-CIS role
  # depends on all three. Pinned to what the role's meta/main.yml lists as
  # compatible, and to what the build was verified against (2026-09-06).
  - name: ansible.windows
    version: "3.0.0"

  - name: community.windows
    version: "3.0.0"

  - name: community.general
    version: "10.0.0"
