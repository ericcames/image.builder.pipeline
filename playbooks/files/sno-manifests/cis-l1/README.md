Pre-rendered CIS L1 MachineConfig manifests for RHCOS nodes.

These are extracted from the Compliance Operator's `ocp4-cis-node` profile
remediations on a reference cluster. See docs/design.md §11.5 for the
extraction process.

Pending: will be populated during Phase 5.3.
